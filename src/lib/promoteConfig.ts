/**
 * Email that receives "Promociona tu negocio" form submissions, delivered via
 * FormSubmit (https://formsubmit.co) — a free form-to-email service that needs
 * no backend and no API key, just this address in the form's action URL.
 *
 * To activate the form:
 * 1. Replace the placeholder below with the real destination email.
 * 2. Submit the form once — FormSubmit sends that address a one-time
 *    confirmation email, and submissions won't arrive until it's clicked.
 */
export const PROMOTE_FORM_EMAIL = "TU-EMAIL-AQUI@ejemplo.com";
